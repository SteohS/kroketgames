# Images

- `animals/` — one PNG per animal (see that folder's README).
- `fruit/` — one PNG per fruit (see that folder's README).
- `icon-512.png` — app icon for the PWA manifest (`manifest.webmanifest`).
  512×512 PNG, used as the home-screen icon when installed via
  Safari → Share → Add to Home Screen.
