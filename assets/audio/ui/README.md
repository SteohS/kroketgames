# UI sounds

Optional overrides for the synthesized WebAudio sounds. If a file is missing,
SoundKit synthesizes a gentle tone instead.

Recognized names:
- success.mp3  — correct answer (keep it warm, not blaring)
- nope.mp3     — wrong answer (deliberately soft & boring)
- pop.mp3      — generic tap/pop (for future games like bubbles/counting)
